# Cloudflare config for Free-tier deployment.
# Site: Cloudflare Pages (static Astro output).
# Optional APIs: bind this Worker/D1 when adding leaderboard.

name = "daily-word-hunt"
compatibility_date = "2026-08-01"
pages_build_output_dir = "dist"

# Uncomment when enabling leaderboard (Workers Paid not required for low traffic).
# [[d1_databases]]
# binding = "DB"
# database_name = "daily-word-hunt"
# database_id = "REPLACE_AFTER_wrangler_d1_create"

# Optional: pointer for "today's puzzle" (write ~1/day — OK on Free KV write limits)
# [[kv_namespaces]]
# binding = "CONFIG"
# id = "REPLACE"
